%read signal
[audioIn,fs]=audioread('Piano.wav');
%choose one column of the matrix if there are harmonies
audioIn = audioIn(:,1);

f0 = pitch(audioIn,fs);

x = 1:1883;
A = f0;
TF = islocalmax(A,'MinSeparation',20);
plot(x,A,x(TF),A(TF),'r*')
ylabel('Pitch (Hz)')
xlabel('Sample Number')
