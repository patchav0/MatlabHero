% https://www.noiiz.com/sounds/instruments/72
% http://www.angelfire.com/sc/wavsetc/index.html

%read signal
[signal,fs]=audioread('Piano.wav');
%choose one column of the matrix if there are harmonies
signal = signal(:,1);
%remove outliers
signal = rmoutliers(signal);
plot(signal);

%Adriannas idea for making notes
sig_max = max(signal);

%split up signal in to logical arrays for each range of notes
A = ((signal > sig_max/5) & (signal < sig_max/2));
B = ((signal < sig_max/5) );

%*** To play sounds

%The original signal
sound(signal,fs)
%Only when the note you want occurs
sound((A.*signal),fs)

